name: 'Setup Node Project'
description: 'Setup Node.js, cache dependencies, and install (call after checkout)'

inputs:
  node-version:
    description: 'Node.js version to use'
    required: false
    default: '18'

runs:
  using: 'composite'
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: 'npm'

    - name: Install Dependencies
      shell: bash
      run: npm ci --ignore-scripts
