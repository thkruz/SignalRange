import { BaseElement } from "./base-element";

export class PowerSwitch extends BaseElement {
  protected html_: string;

  protected addEventListeners_(): void {
    throw new Error("Method not implemented.");
  }
}