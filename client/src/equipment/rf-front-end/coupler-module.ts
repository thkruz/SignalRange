import { CouplerState } from './rf-front-end';

export class CouplerModule {
  state: CouplerState;
  constructor(state: CouplerState) {
    this.state = state;
  }
  // Add UI and logic methods here
}
